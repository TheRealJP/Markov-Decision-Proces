class PolicyWriter:
    @classmethod
    def write(cls, policy):
        """
        Writes out the agent's policy.
        :param policy: the agent's policy.
        """
        raise NotImplementedError()
